# 任务：搭建「英语精读」APP 的项目骨架

你好。我要做一个面向大学生的英语精读订阅 APP。我主要靠 AI 写代码，自己会一点前端。
这次只搭**项目的基本逻辑和架构**：数据怎么组织、流程怎么走、状态怎么存、页面怎么串起来。
精细的界面设计和交互细节，之后我朋友会给我，到时候再改。所以这次的原则是：

- **逻辑要对、结构要清楚、以后好改**
- **界面只要能用、清爽，不做精细视觉**

请先读完这份文档和下面列出的材料，再动手。

---

## 1. 产品一句话

每周推几篇"精读文章"。每篇是一段原创英文（300–450 词）加全套拆解：中文导读、段落大意、重点词、长难句、同义改写题等。
用户读完一篇要走一个固定的学习流程（见第 5 节），收藏的词和句子第二天用间隔复习推回来。
文章可以属于一个"拆书系列"（比如把一本书拆成 5 期）。

## 2. 当前目录里已有的材料

- `content/articles/hkp-01.json`：第一篇文章的完整数据，**以它的结构为准**
- `content/series/how-to-know-a-person.json`：它所属的系列数据
- `docs/demo-reference.html`：一个网页版交互原型。**流程逻辑以它为准，视觉不用照搬**。可以读里面的 JS 看每一步怎么判断

初始化 Expo 项目时，保留这些文件和本文件（可以先在临时目录创建项目，再合并过来）。

## 3. 技术选型（已定，请不要替换）

- Expo（最新稳定版 SDK，保证能用手机上的 Expo Go 打开）+ TypeScript（strict）
- 路由：expo-router
- 状态：zustand + persist，存储用 @react-native-async-storage/async-storage
- 内容校验：zod
- 朗读：expo-speech（系统语音）
- 英文正文字体：@expo-google-fonts/lora；中文用系统字体
- 测试：jest-expo，只测纯逻辑，不写 UI 测试

**没有后端、没有登录、没有支付。** 要加上面没列的库，先问我。

## 4. 架构要求

目录大致这样，可以按需要微调，但要在 CLAUDE.md 里写清楚最终结构：

```
app/                      只放路由和页面拼装，不写业务逻辑
  _layout.tsx
  (tabs)/_layout.tsx      底部三个标签：今日 / 知识库 / 单词
  (tabs)/index.tsx        今日
  (tabs)/library.tsx      知识库
  (tabs)/words.tsx        单词（收藏 + 复习入口）
  series/[id].tsx         系列详情
  article/[id].tsx        文章页（五步学习流程都在这一页里切换）
  review.tsx              复习
  settings.tsx            设置
content/                  文章和系列的 JSON，外加自动生成的 index.ts
src/
  content/                类型定义、zod schema、ContentRepository
  store/                  zustand store（用户数据）
  logic/                  纯函数：学习流程、分流、复习调度、今日选文、连续天数、日期工具
  components/             可复用组件
  theme/                  颜色、字号、间距的 tokens
scripts/                  内容校验 + 生成 content/index.ts
```

几条硬规矩：

1. **数据访问走接口**。定义 `ContentRepository`（getAllSeries / getSeries / getPublishedArticles / getArticle 之类），现在用 `LocalContentRepository` 读打包的 JSON。以后要换成云端（大概率是腾讯云 CloudBase），只加一个新实现，页面代码不动。
2. **业务规则全部写成 `src/logic/` 里的纯函数**，页面只调用。第 5–8 节的规则都要有单元测试。
3. **样式全部走 theme tokens**，不许在组件里写死颜色和字号，方便以后整体换设计。只做浅色主题。参考色：背景 #EFE9DE、卡片 #FFFDF8、正文 #1B2130、次要文字 #72737B、主色 #24345E；三种练法：精读补上 #9A5B12、读准 #3F7A5A、挑战 #4F45A6。
4. 预留一个 `track(event, props)` 埋点函数，现在只 console.log。开始读、读完、检测得分、切换练法、收藏、复习评分都调用它。
5. 界面文字用中文。关键逻辑写中文注释，我要能看懂。

## 5. 内容层

- 从两个 JSON 样例推出 TypeScript 类型和对应的 zod schema（Article、Series 以及它们的子结构）。样例里的 `intro` 字段现在不用，设为可选。
- Metro 不支持动态读取文件夹，所以写一个 Node 脚本：扫描 `content/articles` 和 `content/series`，**先校验，再生成 `content/index.ts`**。挂到 `npm run content`，并在 `npm start` 之前自动执行。
- 校验除了 schema，还要查这些"对不上"的问题，发现就报错并指出文件和字段：
  - `vocab[].word` 必须原样出现在对应段落的英文里
  - `sentences[].text`、`questions[].answer` 必须是对应段落英文的原样子串
  - `check[].answer` 在选项范围内
  - `headings.answers` 覆盖每一段，且下标在选项范围内
  - 文章的 `seriesId` 必须存在；系列 `issues` 里引用的 `articleId` 如果写了，必须存在
- **发布规则**：`publishAt`（YYYY-MM-DD，按本地时区）晚于今天的文章不显示。设置页加一个仅开发环境可见的开关"预览未发布内容"。

## 6. 学习流程（每篇文章）

步骤依次是：**定向 → 裸读 → 检测 → 练习**。复习是全局的，不属于某一篇。

1. **定向**：显示 `orientation.background` 和 `orientation.question`，按钮"开始裸读"。
2. **裸读**：倒计时 `rawReadSeconds` 秒；正文不加任何标注，不显示中文。点"读完了"时记录实际用时。时间到了只提示，不强制跳转。
3. **检测**：`check` 里的选择题。全部答完才能提交，提交后显示对错和解析，不能改。
4. **分流**：得分率 = 答对数 / 题数。全对 → **挑战**；答对一半及以上 → **读准**；否则 → **精读补上**。（2 道题时就是 2 对挑战、1 对读准、0 对精读补上。）
5. **练习**：进入推荐练法，用户可以随时切换另外两种。
   - **精读补上**：两个开关，"基础"显示每段中文大意，"进阶"标出重点词和长难句。点词弹出底部卡片，显示音标、本文释义、级别、所在句子、"上次遇到"（见第 7 节），可以收藏；点长难句展开拆解和译文，可以收藏这句。另有朗读，按段读，读到哪段高亮哪段。
   - **读准**：显示 `questions`；点"在原文中标出答案"，高亮原句并显示解析。
   - **挑战**：每段上方一个下拉框，从 `headings.options` 里选小标题；全部选完才能检查，检查后显示对错和参考答案。
   - 共同部分：一句话输出（模板 + 范例，用户填了就存本地）、本周小练习、读后反馈（多选标签）、"读完了"按钮。
6. 点"读完了" = 这篇完成，记录完成时间，并算一次当天打卡。
7. **每篇都重新检测、重新分流，不给用户存"固定水平"。**
8. 每篇的进度都要保存（当前步骤、裸读用时、检测选项和得分、当前练法、小标题选择、已显示的答案），退出再进要能接着来。设置里能重置某一篇的进度。

## 7. 收藏与复习

- 收藏项分两种：**词**（lemma、原文写法、音标、释义、所在句子、articleId、paragraphId）和**句子**（原文、译文、articleId、paragraphId）。
- 新收藏：等级 0，下次复习日期 = 明天。
- 复习间隔按等级：`[1, 2, 4, 7, 15, 30]` 天。评分规则：
  - 记得：等级 +1，下次复习 = 今天 + 新等级对应的天数；等级到 6 算已掌握，不再出现
  - 模糊：等级不变，明天再来
  - 忘了：等级回 0，明天再来
- 今天要复习的 = 下次复习日期 ≤ 今天、且没掌握的。
- 复习卡：词卡正面是**原句挖空 + 中文提示**，背面是词、音标和完整原句；句子卡正面是**中文译文**，背面是英文原句。复习完一轮算一次当天打卡。
- **上次遇到**：任何文章里点开一个词，如果这个 lemma 已经收藏过，就显示当时收藏的那句话和日期；没有就显示"第一次遇到这个词"。
- 单词页：顶部显示"今天要复习 N 个"和开始按钮，下面分"学习中 / 已掌握"列出全部收藏。点一项进详情，能跳回原文那一段。

## 8. 今日页、知识库、连续天数

- **今日**：已发布、还没读完的文章里最新的一篇；全部读完就显示"去复习 N 个"。同时显示连续打卡天数和今天待复习数。
- **知识库**：上面是系列卡片（显示已读 x / 共 y 期），下面是单篇列表；能按栏目（思想 / 资讯 / 专业）筛选。每项显示状态：未读 / 读到第几步 / 已完成。
- **系列详情**：按样例 JSON 显示一句话介绍、适合什么时候读、关键洞察、思维导图（树形列表就行）、各期目录、名词卡。未发布或未完成的期显示"即将上线"。
- **连续天数**：有打卡的日期连成的天数，以今天或昨天结尾才算连续；否则为 0。
- 所有日期按本地时区的 YYYY-MM-DD 处理，写一个小的日期工具，不引入日期库。

## 9. 这次不做

登录注册、付费和兑换码、云同步、推送通知、分享图片导出、评论社区、深色模式、后台管理、上架应用商店。

## 10. 工作方式

请分阶段做。每个阶段结束：跑通 `npm run content` 和 `npm test`，用 git 提交一次，并告诉我怎么在手机上看到效果。

- **阶段 0**：读完材料，给我计划、最终目录结构和类型定义草稿。**先不要写代码，等我确认。**
- **阶段 1**：初始化 Expo 项目、路由骨架、theme tokens、CLAUDE.md。手机上能打开，能在三个标签之间切换。
- **阶段 2**：内容层：类型、zod、ContentRepository、校验和生成脚本。
- **阶段 3**：store 和 `src/logic/` 里的纯函数（学习流程、分流、复习调度、今日选文、连续天数），附单元测试。
- **阶段 4**：把页面串起来：今日、知识库、系列详情、文章页五步流程、单词、复习、设置。
- **阶段 5**：按第 11 节自测，写 README（怎么安装、怎么运行、怎么加一篇新文章）。

遇到这份文档没写清楚的**产品问题**，先列出来问我，不要自己编规则。**技术上的小选择**可以自己定，在 CLAUDE.md 里记一笔。

CLAUDE.md 要写清楚：目录结构、各层职责、内容 JSON 的格式、怎么加新文章、第 6–8 节的业务规则摘要。以后每次新对话都靠它接上。

## 11. 验收清单

- [ ] `npm start` 后，手机 Expo Go 扫码能打开，三个标签都能切换
- [ ] 在 content 里放一个字段写错的 JSON，`npm run content` 能报出具体哪个文件哪个字段
- [ ] 第 1 篇能从定向走到练习；答对 0 / 1 / 2 道分别进入精读补上 / 读准 / 挑战；三种练法能互相切换
- [ ] 中途退出 APP 再打开，文章停在原来的步骤
- [ ] 收藏一个词和一个句子后，把手机日期往后调一天，复习页能看到它们，评分后下次日期正确
- [ ] 读完一篇后，今日页的连续天数 +1
- [ ] 把文章的 `publishAt` 改成明天，它从今日和知识库里消失；打开"预览未发布内容"后又出现
- [ ] `src/logic/` 的单元测试全部通过
